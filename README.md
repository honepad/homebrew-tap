# honepad tap

```bash
brew tap honepad/tap
brew trust honepad/tap
brew install honepad/tap/honepad
```

Each GitHub Release of [honepad/honepad](https://github.com/honepad/honepad)
rewrites `Formula/honepad.rb`. The committed formula is the source of
truth. `brew upgrade honepad` after `brew update`.
